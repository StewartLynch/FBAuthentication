import XCTest
@testable import FBAuthentication

final class FBAuthenticationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(FBAuthentication().text, "Hello, World!")
    }
}
